import Image from 'next/image';import Link from 'next/link';import {notFound} from 'next/navigation';import {ArrowLeft,Heart,MapPin,ShieldCheck,MessageCircle,Phone,Flag} from 'lucide-react';import {listings,rupiah} from '@/lib/data';
export default async function Listing({params}:{params:Promise<{id:string}>}){const {id}=await params;const item=listings.find(x=>x.id===id);if(!item)return notFound();return <main className="container py-8"><Link href="/" className="text-sm font-bold text-gray-500 flex gap-2 items-center"><ArrowLeft className="w-4 h-4"/>Kembali</Link><div className="grid lg:grid-cols-[1.4fr_.8fr] gap-8 mt-6"><div><div className="card overflow-hidden aspect-[4/3] relative"><Image src={item.image} alt={item.title} fill className="object-cover"/></div><div className="card p-6 mt-5"><h2 className="font-black text-xl">Deskripsi</h2><p className="text-gray-600 mt-4 leading-7">Barang dalam kondisi {item.condition.toLowerCase()}. Silakan chat penjual untuk memastikan detail, ketersediaan, lokasi COD, dan informasi tambahan sebelum transaksi.</p></div></div><div><div className="card p-6 sticky top-24"><div className="flex justify-between"><span className="pill">{item.category}</span><button><Heart/></button></div><h1 className="text-2xl font-black mt-4">{item.title}</h1><div className="text-3xl font-black mt-3">{rupiah(item.price)}</div><div className="text-gray-500 text-sm mt-4 flex items-center gap-2"><MapPin className="w-4 h-4"/>{item.location}</div><div className="grid grid-cols-2 gap-3 mt-6 text-sm"><div className="bg-gray-50 rounded-xl p-3"><span className="text-gray-400 block">Kondisi</span><b>{item.condition}</b></div><div className="bg-gray-50 rounded-xl p-3"><span className="text-gray-400 block">Kategori</span><b>{item.category}</b></div></div><div className="border-t mt-6 pt-6 flex gap-3"><div className="w-11 h-11 rounded-full bg-green-100 flex items-center justify-center font-black text-green-700">{item.seller[0]}</div><div><b>{item.seller}</b><div className="text-xs text-gray-500 flex gap-1 items-center mt-1">{item.verified&&<><ShieldCheck className="w-3 h-3 text-green-600"/>Terverifikasi</>}</div></div></div><Link href="/chat" className="btn btn-primary w-full mt-6"><MessageCircle className="w-4 h-4"/>Chat Penjual</Link><button className="btn w-full mt-2 border"><Phone className="w-4 h-4"/>Tampilkan Kontak</button><button className="w-full text-xs text-gray-400 mt-5 flex justify-center gap-1"><Flag className="w-3 h-3"/>Laporkan iklan</button></div></div></div></main>}
