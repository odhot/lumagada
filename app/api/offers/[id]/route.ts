import {NextResponse} from 'next/server';
import {createClient} from '@/lib/supabase/server';
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:'Login diperlukan.'},{status:401});const body=await request.json().catch(()=>({}));const status=String(body.status||'');if(!['accepted','rejected'].includes(status))return NextResponse.json({error:'Status penawaran tidak valid.'},{status:422});const {data,error}=await supabase.from('offers').update({status}).eq('id',id).eq('seller_id',user.id).eq('status','pending').select('id,listing_id,amount,status,updated_at').maybeSingle();if(error)return NextResponse.json({error:error.message},{status:400});if(!data)return NextResponse.json({error:'Penawaran tidak ditemukan atau sudah diproses.'},{status:404});return NextResponse.json({data})}
