import {NextResponse} from 'next/server';import {createClient} from '@/lib/supabase/server';import {isRestrictedListing,restrictedServiceMessage} from '@/lib/policy';

export async function GET(request:Request){const supabase=await createClient();const u=new URL(request.url);const q=u.searchParams.get('q')?.trim()||'';const city=u.searchParams.get('city')?.trim()||'';const category=u.searchParams.get('category')?.trim()||'';const limit=Math.min(Math.max(Number(u.searchParams.get('limit')||24),1),100);const offset=Math.max(Number(u.searchParams.get('offset')||0),0);let query=supabase.from('listings').select('id,title,description,price,condition,city,district,latitude,longitude,contact_phone,status,urgent,is_pro_listing,accepts_offers,expires_at,created_at,listing_images(url,sort_order),profiles(display_name,verified,seller_type)').eq('status','active').order('created_at',{ascending:false}).range(offset,offset+limit-1);if(q)query=query.textSearch('search_vector',q,{config:'simple',type:'websearch'});if(city)query=query.eq('city',city);if(category){const {data:cat}=await supabase.from('categories').select('id').eq('name',category).maybeSingle();if(cat)query=query.eq('category_id',cat.id)}const {data,error}=await query;if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({data:data||[],pagination:{limit,offset,count:data?.length||0}})}

export async function POST(request:Request){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:'Login diperlukan.'},{status:401});let body:any;try{body=await request.json()}catch{return NextResponse.json({error:'JSON tidak valid.'},{status:400})}const title=String(body.title||'').trim(),description=String(body.description||''),condition=String(body.condition||'Bekas'),category=String(body.category||''),city=String(body.city||'').trim(),price=Number(body.price);if(!title||!city||!Number.isFinite(price)||price<0)return NextResponse.json({error:'Judul, lokasi, dan harga wajib diisi.'},{status:422});if(isRestrictedListing({title,description,condition,category}))return NextResponse.json({error:restrictedServiceMessage},{status:422});const {data:cat}=await supabase.from('categories').select('id').eq('name',category).maybeSingle();const {data,error}=await supabase.from('listings').insert({seller_id:user.id,category_id:cat?.id??null,title,description,condition,price,city,district:body.district||null,latitude:body.latitude??null,longitude:body.longitude??null,contact_phone:body.contact_phone||null,accepts_offers:condition==='Lowongan'?false:body.accepts_offers!==false,status:'active'}).select('id').single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({data},{status:201})}
