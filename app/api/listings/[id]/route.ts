import {NextResponse} from 'next/server';import {createClient} from '@/lib/supabase/server';
export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const supabase=await createClient();const {data,error}=await supabase.from('listings').select('id,title,description,price,condition,city,district,latitude,longitude,contact_phone,status,urgent,is_pro_listing,accepts_offers,expires_at,created_at,updated_at,listing_images(id,url,sort_order),profiles(id,display_name,verified,seller_type,seller_verified,seller_business_name,city)').eq('id',id).maybeSingle();if(error)return NextResponse.json({error:error.message},{status:400});if(!data)return NextResponse.json({error:'Iklan tidak ditemukan.'},{status:404});return NextResponse.json({data})}
