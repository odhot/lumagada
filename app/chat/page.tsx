'use client';import {useState} from 'react';import {Send,ArrowLeft} from 'lucide-react';import Link from 'next/link';
export default function Chat(){const [text,setText]=useState('');const [msgs,setMsgs]=useState(['Halo! Apakah barangnya masih tersedia?','Masih tersedia, kak. Bisa COD di Jakarta Selatan.']);return <main className="container py-8 max-w-3xl"><Link href="/" className="text-sm text-gray-500 flex items-center gap-2"><ArrowLeft className="w-4 h-4"/>Kembali</Link><div className="card mt-5 overflow-hidden"><div className="p-5 border-b"><b>Chat dengan Raka Store</b><div className="text-xs text-green-600 mt-1">● Online</div></div><div className="h-[430px] p-5 space-y-3 bg-[#f7faf8]">{msgs.map((m,i)=><div key={i} className={`max-w-[75%] p-3 rounded-2xl text-sm ${i%2?'bg-green-600 text-white ml-auto':'bg-white border'}`}>{m}</div>)}</div><div className="p-3 border-t flex gap-2"><input className="input" value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&text.trim()){setMsgs([...msgs,text]);setText('')}}} placeholder="Tulis pesan..."/><button className="btn btn-primary" onClick={()=>{if(text.trim()){setMsgs([...msgs,text]);setText('')}}}><Send className="w-4 h-4"/></button></div></div></main>}
