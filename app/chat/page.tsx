'use client';
import {useState} from 'react';import {Send,ArrowLeft,Tag,ShieldCheck} from 'lucide-react';import Link from 'next/link';import {useSearchParams} from 'next/navigation';
export default function Chat(){const p=useSearchParams();const seller=p.get('seller')||'Raka Store',listing=p.get('listing')||'iklan ini',offer=p.get('offer');const [text,setText]=useState(''),[msgs,setMsgs]=useState<string[]>([offer?`Saya ingin mengajukan penawaran ${new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(Number(offer)).replace('IDR','Rp')} untuk ${listing}.`:`Halo! Apakah ${listing} masih tersedia?`,'Masih tersedia, kak. Bisa dibicarakan.']);const send=()=>{if(!text.trim())return;setMsgs(v=>[...v,text.trim()]);setText('')};return <main className="container py-8 max-w-3xl"><Link href="/" className="text-sm text-gray-500 flex items-center gap-2"><ArrowLeft className="w-4 h-4"/>Kembali</Link><div className="card mt-5 overflow-hidden"><div className="p-5 border-b"><div className="flex items-center gap-2"><b>Chat dengan {seller}</b><ShieldCheck className="w-4 h-4 text-green-600"/></div><div className="text-xs text-green-600 mt-1">● Online · {listing}</div></div><div className="p-3 bg-[#edf8ef] border-b text-xs flex items-center gap-2"><Tag className="w-4 h-4 text-green-700"/><span>Negosiasi lebih mudah — penawaran harga bisa dikirim langsung lewat chat.</span></div><div className="h-[430px] p-5 space-y-3 bg-[#f7faf8] overflow-y-auto">{msgs.map((m,i)=><div key={i} className={`max-w-[75%] p-3 rounded-2xl text-sm ${i%2?'bg-green-600 text-white ml-auto':'bg-white border'}`}>{m}</div>)}</div><div className="p-3 border-t flex gap-2"><input className="input" value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')send()}} placeholder="Tulis pesan..."/><button className="btn btn-primary" onClick={send}><Send className="w-4 h-4"/></button></div></div></main>}
